# Finish jGuard
# cleanup frontend api stack
# lock down billing routes/pages when module is disabled (e.g. renewals)


# Option A – NestJS + Prisma + PostgreSQL: Striktes Domain-Layer, DTO-Validation mit Zod/Class-Validator, BullMQ für Queue, Stripe-SDK + Webhooks, GraphQL/tRPC möglich; passt zur bestehenden TypeScript-Expertise, leicht mit Next.js zu kombinieren.